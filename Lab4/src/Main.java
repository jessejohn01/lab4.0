/**
 * Kyle Webster and Jesse Arstein
 * Finished November 28, 2017
 */
public class Main {
	 public static void main(String[] args) {
		 DivideAndConquer mainInstance = new DivideAndConquer(); // Creates the instance that runs the algorithm.
		 mainInstance.buildNodes(); //builds the Nodes from the list of points
		 mainInstance.shortestDistance(); //finds the points that have the shortest distance from each other
	 }
}
